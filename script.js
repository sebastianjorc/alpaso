/*// Accede al botón "Ordena Ahora"
const orderButton = document.querySelector('.cta');

// Agrega un evento al hacer clic en el botón
orderButton.addEventListener('click', function(event) {
  // Previene el comportamiento predeterminado del botón
  event.preventDefault();

  // Coloca aquí tu código para procesar la orden
});

// Accede al formulario de contacto
const contactForm = document.querySelector('form');

// Agrega un evento al enviar el formulario
contactForm.addEventListener('submit', function(event) {
  // Previene el comportamiento predeterminado del formulario
  event.preventDefault();

  // Accede a los campos del formulario
  const nameInput = document.querySelector('#nombre');
  const emailInput = document.querySelector('#email');
  const messageInput = document.querySelector('#mensaje');

  // Coloca aquí tu código para enviar el formulario
});
*/